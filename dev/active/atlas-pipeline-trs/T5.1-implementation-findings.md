# T5.1 Implementation Findings

**Branch:** `claude/brave-lamport-fda4f2`  
**Date:** 2026-05-05 -> 2026-05-06 (updated 2026-05-06)  
**Status:** PARTIALLY COMPLETE - major infra fixes landed; plumb durability acceptance still open

---

## Purpose

Document what the T5.1 branch changed to enable end-to-end pipeline execution, what is now verified, and what remains to satisfy the original T5.1 success criteria.

---

## Confirmed Implementation Wins

### Plugin invocation and routing correctness

- Replaced invalid `claude --slash ...` calls with `claude -p ... --no-session-persistence`.
- Added prompt construction support for slash command and raw prompt modes in `src/atlas/plugin_resolver.py`.
- Corrected plugin command mappings, including namespaced `DEV-ESSENTIALS:*` commands.
- Updated subprocess execution to run from atlas root with `--add-dir` access for target repo/worktree.

### Pipeline control flow hardening

- Added `AutoPrompter` and `--auto-approve` support to `atlas run` and `atlas resume`.
- Added `awaiting_hook` wait behavior and repeat-attempt cap in `Pipeline.run_to_completion()`.
- Persisted and rehydrated `code_gen_span_id` via `.atlas/current-run` so hook score attribution can be recovered on resume.

### Hook and run lifecycle robustness

- Fixed `PlumbIO.close_run()` to exit the run context manager (`_run_ctx`) instead of the run handle.
- Hardened hook script generation to bake install-time interpreter and include fallback checks.
- Added warning when hook interpreter path is outside repo `.venv`.

---

## Current Validation Snapshot

### Automated test results (current branch/session)

- `uv run pytest tests/unit/test_remediation.py tests/unit/test_review_fixes.py -q` -> `29 passed`
- `uv run pytest -q` -> `111 passed`
- `uv run pytest --cov=src/atlas --cov-report=term-missing --cov-fail-under=80` -> `111 passed`, `92.34%` total coverage

### Important caveat

- These passing tests are mostly stub-mode validations.
- Real plumb durability semantics are still not fully runtime-proven in this environment because `plumb` is not importable in the atlas `uv run` environment.

---

## Open Acceptance Gaps

### 1) Real plumb resume durability remains unresolved (P0)

- `Pipeline.resume()` calls `reopen_run(run_id)` but does not consume the returned run id.
- `PlumbIO.reopen_run()` currently opens a new run-like context (`plumb_run(task_id=run_id, kind="online")`) rather than clearly reattaching to the original active run.
- Parent/child linkage semantics are not explicitly enforced in atlas code.

### 2) Resume task context fidelity is still wrong (P1)

- Resume reconstructs `ctx.task` from tasks header/slug path, not from persisted original task text.
- This can degrade plugin prompt quality after resume.

### 3) Rejection examples are still stub-only durable (P1)

- In real mode, `PlumbIO.write_example()` creates an `Example` but only appends to local `self.examples`.
- No durable write call through plumb storage is performed.

### 4) Hook idempotency guard is still missing (P2)

- Hook appends pending score records without dedupe.
- Repeated post-commit invocations for the same commit can duplicate `gate_commit` scores.

### 5) Span latency remains placeholder data (P2)

- `Pipeline.step()` still records `latency_ms=0.0`.

### 6) Same-process context drift can still happen (P2)

- Updated `worktree_path` context from `step()` is not propagated back to caller-owned `ctx` in `run_to_completion()`.
- This can cause `code_review` to run against repo root in same-process flow.

---

## T5.1 Criteria Status (Reconciled)

- Criterion 4 (main branch isolation): PASS
- Criterion 5 (resume protocol basic continuation): PASS
- Criteria 1-3 (durable plumb rows and attribution): NOT YET ACCEPTED

---

## Next Steps To Close T5.1

1. Implement and verify real plumb resume linkage semantics (reattach or explicit child-run model), and track active run id across resume.
2. Persist original task text explicitly and rehydrate it on resume.
3. Route `write_example()` through real plumb storage in real mode.
4. Add hook dedupe using `(run_id, commit_sha, metric)` plus replay test.
5. Measure and emit real stage runtime for `latency_ms`.
6. Fix same-process context propagation so stage 6 always uses worktree context after stage 5.
7. Re-run manual T5.1 acceptance with importable plumb and verify criteria 1-5 end to end.
