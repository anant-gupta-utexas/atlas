[project]
name = "atlas"
version = "0.1.0"
description = "Local-CLI agent orchestrator + metrics logger for a phase-gated dev workflow."
readme = "README.md"
requires-python = ">=3.11"
dependencies = []
